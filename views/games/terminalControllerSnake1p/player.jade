extends layout


block content

	script role = 'controller'

	#controller.section
		h1 Touch to move!
		#marker
			.down.marker
			.move.marker

	#finished.section
		h1 Game Over
		button.again Again!
		button.exit Exit!